/**
 * Intentando hacer una escena
 * @extends Phaser.escena
 */
 export default class escenaMenu extends Phaser.escena {
     _
    constructor() {

      
      super({ key: 'menuScene' });       
      {
      }; 
      
    }

    preload()
    {
        this.loadWallpaper();
    }
}

